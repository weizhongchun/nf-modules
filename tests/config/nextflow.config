params {
    outdir = "output/"
    publish_dir_mode = "copy"
    singularity_pull_docker_container = false
}

process {
    // resourceLimits requires Nextflow >= 24.10.0 (see manifest.nextflowVersion on line 47)
    resourceLimits = [
        cpus: 4,
        memory: '16.GB',
        time: '1.h'
    ]
}

// Default to Docker for local tests; specific profiles (e.g. 'singularity', 'conda') are defined in `nf-test.config`
docker.enabled = true
docker.fixOwnership = true
docker.runOptions = '--platform=linux/amd64'

docker.registry      = 'quay.io'
podman.registry      = 'quay.io'
singularity.registry = 'quay.io'

// Increase time available to build Conda environment
conda { createTimeout = "120 min" }

// Load test_data.config containing paths to test data
includeConfig 'test_data.config'

manifest {
    nextflowVersion = '>=24.10.2'
}
